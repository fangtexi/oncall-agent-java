spring:
  application:
    name: oncall-agent-java
# 文档分片配置
document:
  chunk:
    max-size: 50
    overlap-size: 100
# 阿里云 DashScope Embedding API 配置
dashscope:
  api:
    key: sk-084ed4bad9d540b2bfa49196c0362a5e  # 从环境变量读取或使用默认值（用于自定义配置）
  embedding:
    model: text-embedding-v4  # 阿里云文本向量化模型